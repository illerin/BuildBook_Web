services:
  parttrack:
    build: .
    ports:
      - "8079:8080"
    volumes:
      - parttrack-uploads:/app/uploads
      - parttrack-pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  parttrack-uploads:
  parttrack-pgdata:
